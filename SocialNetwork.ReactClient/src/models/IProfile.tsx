export interface IProfile {
    displayName: string,
    username: string,
    mainPhoto: string,
    bio: string,
    // IsFollowed:string
    followersCount: string,
    followingCount: string
    photos: string[],
}